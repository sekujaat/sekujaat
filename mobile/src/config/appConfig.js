// src/config/appConfig.js

import { APP_NAME, APP_VERSION } from "./env";

export const appConfig = {
  name: APP_NAME,
  version: APP_VERSION,
  theme: {
    primary: "#2563eb",
    background: "#ffffff",
    text: "#0f172a",
  },
};