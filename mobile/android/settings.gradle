pluginManagement {
    repositories {
        mavenCentral()
        google()
        gradlePluginPortal()
    }
}

plugins {
    id("com.facebook.react.settings") version "0.76.5"
}

rootProject.name = "SynceMono"

// React Native projects ko include karo
include(":app")

// React settings plugin ko apply karo
apply(plugin = "com.facebook.react.settings")
